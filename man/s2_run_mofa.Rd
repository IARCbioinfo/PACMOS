% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/s2_run_MOFA.R
\name{s2_run_mofa}
\alias{s2_run_mofa}
\title{Step 2: Train MOFA model for reference + query sample}
\usage{
s2_run_mofa(
  models_dir,
  matrices_subdir = "inputs",
  num_factors = 10,
  convergence_mode = "slow",
  maxiter = 10000,
  use_basilisk = FALSE,
  skip_existing = TRUE,
  python_bin,
  outfile_prefix = "",
  views_map,
  binary_views = NULL
)
}
\arguments{
\item{models_dir}{RRoot directory folder. Same as `s1_add_sample_to_mofa() outdir`..}

\item{matrices_subdir}{Folder name where `.RData` files are stored. (`inputs` by default)}

\item{num_factors}{Integer. Number of latent factors.}

\item{convergence_mode}{MOFA convergence mode (`"slow"` or `"fast"`).}

\item{maxiter}{Maximum number of training iterations.}

\item{use_basilisk}{Logical. Whether to run MOFA inside basilisk environment.}

\item{skip_existing}{Logical. If TRUE, skip samples for which output
HDF5 already exists.}

\item{python_bin}{Path to the Python binary used by the MOFA
environment via the `reticulate` package.}

\item{outfile_prefix}{Optional prefix for output files.}

\item{views_map}{Named character vector mapping MOFA view names to the corresponding matrix object names
loaded from `.RData` files. Names define the view labels (e.g. "RNA"), and values specify the matrix objects
(e.g. "D_expr_MOFA"). The view names are only labels and do not affect the analysis.

Example:
\preformatted{
views_map = c(
  RNA = "D_expr_MOFA",
  CNV = "D_cnv_MOFA",
  ALT = "D_alt_MOFA"
)
}}

\item{binary_views}{Character vector specifying which views contain
binary data (e.g. mutation or alteration layers).}
}
\value{
Invisibly returns TRUE when MOFA training completes. Trained MOFA models are
written to disk as `.hdf5` files.
}
\description{
This function trains a MOFA2 model using the matrices generated in
\code{s1_add_sample_to_mofa()}.
}
\details{
The function loads layer-specific `.RData` matrices, constructs a MOFA object,
trains the model, and writes the resulting model to an HDF5 file.

Each sample is processed independently. For each sample directory, the
corresponding matrices are loaded, aligned across views, and used to train
a MOFA model.

If `skip_existing = TRUE`, samples for which a model file already exists
will be skipped.
}
